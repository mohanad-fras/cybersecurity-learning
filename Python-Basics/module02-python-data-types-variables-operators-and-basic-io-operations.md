# PE1: Module 2 — Python Data Types, Variables, Operators and Basic I/O Operations

## Module Overview

This module focuses on the basic building blocks used when writing Python programs.

## Topics

### Data Types

Python provides different data types for storing different kinds of values.

- `int` - whole numbers
- `float` - decimal numbers
- `str` - text
- `bool` - `True` or `False`

Example:

```python
age = 20
height = 1.75
name = "Mohanad"
is_student = True
```

### Variables

A variable is a name used to store a value.

```python
name = "Mohanad"
age = 20
```

The value of a variable can be changed during program execution.

### Operators

Common arithmetic operators in Python:

| Operator | Meaning |
|---|---|
| `+` | Addition |
| `-` | Subtraction |
| `*` | Multiplication |
| `/` | Division |
| `//` | Floor division |
| `%` | Modulo |
| `**` | Exponentiation |

Example:

```python
a = 10
b = 3

print(a + b)
print(a - b)
print(a * b)
print(a / b)
print(a % b)
```

### Basic Input and Output

`print()` is used to display information on the screen.

```python
print("Hello Python")
```

`input()` is used to receive text from the user.

```python
name = input("Enter your name: ")
print(name)
```

The value returned by `input()` is a string by default. Values can be converted when needed.

```python
age = int(input("Enter your age: "))
```

### Type Conversion

Common conversion functions include:

- `int()`
- `float()`
- `str()`

Example:

```python
number = "10"
number = int(number)
print(number + 5)
```

## What I Practiced

- Working with basic Python data types.
- Creating and changing variables.
- Using arithmetic operators.
- Printing values and messages.
- Reading input from the user.
- Converting values between basic data types.

## Status

🟡 In Progress

## Source

Python Essentials 1 - Cisco Networking Academy / Python Institute
